# SonarCloud project configuration
# Replace placeholders below with your SonarCloud values.

sonar.projectKey=Majidullask04_flappy-bird-react
sonar.organization=YOUR_ORG_KEY
sonar.host.url=https://sonarcloud.io
sonar.sources=src
sonar.language=js
sonar.sourceEncoding=UTF-8

# Note: Do NOT put your token here. Set a repository secret named SONAR_TOKEN
# and expose it to the workflow step as the env variable SONAR_TOKEN.
# Example workflow step already sets env: SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
